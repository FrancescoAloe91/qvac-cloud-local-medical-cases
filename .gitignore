__pycache__/
*.pyc
.venv/
venv/
data/wallet.json
.session_rankings.json
.case_snapshots.json
.ui_prefs.json
.streamlit/secrets.toml
.env
.cache/

# Private History — prompts, model outputs, judge scores, multi-run means.
# Never commit. Each OpenRouter key gets artifacts/owners/<fingerprint>/.
artifacts/
artifacts/**

sidecar/node_modules/
sidecar/vendor/

# Local MedPsy / peer inference weights (downloaded from Hugging Face, not git)
.ollama-bin/
.ollama-models/
models/*.gguf
models/**/*.gguf
models/.cache/
models/.cache/**

# Local tooling (GitHub CLI binary etc.), not part of the project
.local-bin/

# Editor / agent junk
.cursor/
.streamlit/credentials.toml
.streamlit_pid
*.swp
.DS_Store

# Local CI log probes (never commit)
.ci_probe/
.ci_probe/**
