# Screenshots (marketing / docs only)

**Non-authoritative / pre-gold.** These PNGs are marketing illustrations only —
**not** live Clinical Composite rankings, not Rebuild means, and not proof of
model quality. Do not cite them as current bench results.

- Live tracks score via the DeepSeek R1 judge (`app.py`,
  `pages/structured_graded.py`).
- Do **not** regenerate these images from `scripts/verify_dashboard_graphics.py`
  or `OLD/legacy_dashboard.py` and present them as current bench results
  (legacy path may use consensus rescale).
- Prefer fresh screenshots from a Rebuild/Multi popup that still show the
  amber honesty block and orange mean footer (track · protocol · pack · N).
