# Adversarial audit — 2026-08-02 (parity · 10-case Comprehension)

Ruthless critic after UX parity + user-facing de-beta. Scoring formulas unchanged.
Wire protocol ids remain `beta-*` for History continuity.

## Product framing

| Area | State |
|---|---|
| Default | `app.py` = **Comprehension** (discursive) |
| Optional secondary | `pages/structured_graded.py` = **Structured · A1–A5** |
| Legacy URL | `pages/comprehension_redirect.py` → redirect to Home when idle |
| Pack | revision **3** · Case **1–10** · acute ED-biased · `comprehension.json` |
| Isolation | `comprehension-v1` never pools with graded History (legacy `beta-*` dual-read) |

## Critical

### C1 — Gold contract ≠ undivided prose
UI previously implied prose was the scoring reference; Freeze scores curated `gold_raw` Q1–A5.
**Mitigation shipped:** Freeze checkbox / captions state QnA gold + claim count + fingerprint; honesty block bullet.

### C2 — Photocopy sections
Unmarked free-form answers may be copied into all five dimensions.
**Mitigation shipped:** Honesty disclose; do not market five independent Structured-like answers.

### C3 — Gold-relative / OpenRouter ≠ consumer / single DeepSeek / amateur N
Worse optics with 10×Multi (“suite win”).
**Mitigation:** Footers carry track + protocol + pack_rev; ban significance language in posts (x-post template).

### C4 — Dual-track / legacy URL
**Mitigation shipped:** Shared sidebar labels Structured as optional; legacy page redirects; never-pool captions on both heroes.

## High

### H1 — ED monoculture pack
Caption acute ED-biased pack rev 3; no general clinical IQ claim.

### H2 — Custom / auto scaffold weak
Custom Freeze caption warns; Multi×all uses curated `pack_slots` only.

### H3 — Same `case_id` for 10 slots
Default Rebuild scope → **balanced_cases**; Multi×all auto-rebuild uses balanced.

### H4 — Judge N/A / repair
Ops reliability panels remain beside clinical mean; corrective retry disclosed in sidebar.

### H5 — Cost at 10×N×roster
Shared spend Yes/Cancel on both tracks (incl. MedPsy-only on Structured).

### H6 — Statistical overclaim
x-post requires pack_rev + case list / suite label; exploratory only.

## Medium

- Primary wire ids are `comprehension-v1` / `comprehension`; legacy `beta-*` History still pools via dual-read.
- `accuracy` field alias remains quote-tweetable — keep Clinical Composite wording in UI.
- True discursive gold / soft-section parse rate = backlog.

## Disclosure checklist

1. Track: **Comprehension** or **Structured A1–A5**
2. Comprehension: pack_rev + protocol wire id
3. Reference-relative · not clinical truth · not official MedPsy blog
4. OpenRouter ≠ consumer web
5. Uncalibrated DeepSeek R1
6. mean±std · N · cohort · scored-only · ops separate
7. Never pool cross-track
